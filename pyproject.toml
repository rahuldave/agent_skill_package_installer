[project]
name = "agent-skill-package-installer"
version = "0.1.0"
description = "Agent skill repository packaging and installability checks."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

